{% extends 'base.html.twig' %}
{% block body %}
<h1>{{ ecole.nom }}</h1>
    <table>
        <td><a href="{{ path('admin_ecole', {'ecole_id': ecole.id}) }}">Ecole</a></td>
        <td><a href="{{ path('admin_promo', {'ecole_id': ecole.id}) }}">Promo</a></td>
        <td><a href="">Perso</a></td>
        <td><a href="{{ path('ecole_show', {'id': ecole.id}) }}">back to {{ ecole.nom }} view</a></td>
    </table>

    <select id="promoSelect">
        {% for promo in ecole.promos %}
            <option value="{{ promo}}" onselect="Myfunction({{ promo }})">{{ promo.nom }}</option>
        {% endfor %}
    </select>

{% endblock %}
{% block javascripts %}
    <script>
       function Myfunction($promo){
           echo = ' <table>',
           '',
           '<td>{ article.nom }}</td>',
           '<td>{{ article.createdAt }}</td>',
           '<td>{{ article.texte }}</td>',
           '{% endfor %}',
           '</table>';
       }
    </script>

{% endblock %}