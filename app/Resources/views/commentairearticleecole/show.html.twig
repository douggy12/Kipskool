{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentairearticleecole</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaireArticleEcole.id }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ commentaireArticleEcole.texte }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commentairearticleecole_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('commentairearticleecole_edit', { 'id': commentaireArticleEcole.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
