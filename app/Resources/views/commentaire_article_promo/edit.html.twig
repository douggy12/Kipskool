{%extends 'base.html.twig' %}

{% block body %}
    <h1>Commentaire_article_promo edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('article_promo_show', {'id': promo.id, 'article_promo_id': article_promo.id}) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
